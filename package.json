{
  "name": "tok-juara",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "bun run --filter='*' typecheck",
    "lint": "oxlint",
    "format": "oxfmt .",
    "format:check": "bunx --bun oxfmt --check ."
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@typescript/native-preview": "latest",
    "oxfmt": "latest",
    "oxlint": "latest"
  }
}
