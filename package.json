{
  "name": "pentas",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:papan:e2e": "bun run --filter=@pentas/papan test:e2e",
    "typecheck": "bun run --filter='*' typecheck",
    "lint": "oxlint",
    "format": "oxfmt '**/*.{ts,tsx,js,jsx}' --no-error-on-unmatched-pattern",
    "format:check": "bunx --bun oxfmt --check '**/*.{ts,tsx,js,jsx}' --no-error-on-unmatched-pattern",
    "build": "bun run build:dalang && bun run build:dalang-worker && bun run build:papan",
    "build:dalang": "bun build packages/dalang/src/index.ts --compile --target=bun --outfile=dist/dalang",
    "build:dalang-worker": "bun build packages/dalang/src/worker/main.ts --compile --target=bun --outfile=dist/dalang-worker",
    "build:papan": "bun build packages/papan/src/index.ts --compile --target=bun --outfile=dist/papan",
    "build:install": "bun run build && mkdir -p ~/.local/bin && cp dist/dalang dist/dalang-worker dist/papan ~/.local/bin/"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@typescript/native-preview": "latest",
    "oxfmt": "latest",
    "oxlint": "latest"
  }
}
